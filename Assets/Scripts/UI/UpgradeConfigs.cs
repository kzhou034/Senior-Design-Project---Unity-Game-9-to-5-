using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpgradeConfigs : MonoBehaviour
{
    [Header("Set to true if player is able to upgrade the stat")]
    public bool reloadTime;
    public bool projectileSpeed;
    public bool projectileCount;
    public bool projectileSpread;
    public bool projectileMomentum;
    public bool projectileDamage;
    public bool projectileAOE;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
